// Criar um array de números desordenados
let numeros = [5, 2, 9, 1, 5];

// Usar o método sort para classificar o array em ordem crescente
numeros.sort();

// Exibir o array ordenado no console
console.log(numeros); // Saída: [1, 2, 5, 5, 9]